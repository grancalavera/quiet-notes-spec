You are working on the quiet-notes project in Ralph loop mode.

CONTEXT FILES:
- specification/requirements.json - All requirements with passes: true/false flags
- progress.txt - Memory from previous iterations

YOUR TASK (complete in order):

1. READ CONTEXT
   - Read specification/requirements.json to see all requirements
   - Read progress.txt to see what was done previously
   - Read recent git log to understand what was implemented

1.5. FORMAT & LINT CLEANUP
   - Run: cd client && pnpm format:fix && pnpm lint:fix
   - If any files changed (check git status), commit: "style: format and lint fixes"
   - This ensures a clean baseline before feature work

2. PICK ONE REQUIREMENT
   - Find the highest priority requirement where "passes": false
   - Requirements are NOT listed in order - implement them in the most appropriate order for the project state
   - Consider dependencies and what's been completed to choose the best next requirement
   - Choose ONLY ONE requirement to implement
   - If all requirements have "passes": true, output PROMISE_COMPLETE and stop

3. WRITE TEST FIRST
   - Before implementing, write a Playwright e2e test that verifies the requirement
   - Use e2e tests ONLY - no unit tests (coarse test harness strategy)
   - Tests go in e2e/specs/ (see CLAUDE.md Playwright TDD section)
   - The test should fail initially (red)
   - Run the test to verify it fails

4. IMPLEMENT THE FEATURE
   - Write minimal code to make the test pass
   - Build thin e2e slices - complete vertical slices from UI to database
   - Follow the requirement steps exactly
   - Keep changes focused and small

5. VERIFY
   - Run the test - it should pass (green)
   - Type check: cd client && pnpm type-check
   - Verify Docker services are healthy: docker ps

5.5. FINAL FORMAT & LINT CHECK
   - Run: cd client && pnpm format:fix && pnpm lint:fix
   - If any files changed, stage them (they'll be included in the feature commit)

6. UPDATE REQUIREMENTS
   - In specification/requirements.json, change "passes": false to "passes": true for the completed requirement
   - Only mark as passing if the test actually passes

7. UPDATE PROGRESS
   - Append (do NOT overwrite) to progress.txt
   - Format: [YYYY-MM-DD] REQ-XXX-YYY: Brief description of what was implemented and any learnings
   - Keep it concise (1-2 sentences max)

8. COMMIT
   - Git commit with message format: "feat(REQ-XXX-YYY): Brief description"
   - Include the updated requirements.json and progress.txt in the commit
   - Commit message should reference the requirement ID

9. CHECK COMPLETION
   - If ALL requirements now have "passes": true, output PROMISE_COMPLETE
   - Otherwise, you're done with this iteration

CRITICAL RULES:
- Work on EXACTLY ONE requirement per iteration
- Write the test BEFORE implementing
- NEVER delete tests - all tests are permanent
- Build thin e2e slices - complete vertical features from UI to database
- Do NOT mark a requirement as passing unless the test passes
- Append to progress.txt after every requirement (never skip this)
- Do NOT implement multiple requirements at once

If you encounter any errors, fix them before proceeding. Each iteration should result in exactly one git commit with one passing requirement.
