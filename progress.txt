[2026-02-08] Echo tracer bullet implemented. PocketBase + Vite stack proven end-to-end. Docker compose working. Commit: 5831b05
[2026-02-11] REQ-AUTH-001 implemented. Username/password auth with PocketBase users collection. React Router with AuthProvider, ProtectedRoute, LoginPage, NotebookPage. Playwright e2e infrastructure bootstrapped. Test passing.
[2026-02-11] REQ-AUTH-003 implemented. ProtectedRoute now passes `state: { from: location }` when redirecting to /login. LoginPage reads `location.state?.from?.pathname` and navigates there after sign-in (defaults to /notebook). E2e test verifies the full redirect round-trip.
[2026-02-15] REQ-AUTH-002 implemented. Added header with profile avatar button and sign-out popover to NotebookPage. Clicking profile opens a popover with Sign Out button. Sign out clears PocketBase authStore and navigates to /login. Learned: emoji content inside buttons can interfere with aria-label accessible name in Chromium — use aria-hidden span instead.
[2026-02-15] REQ-AUTH-004 implemented. Added auth guard to LoginPage — if user is already authenticated, redirects to the intended destination (from location state) or /notebook via React Router's Navigate component.
[2026-02-15] REQ-AUTH-005 implemented. AuthProvider now shows a loading indicator (role="status", aria-label="Loading") while validating auth via pb.authRefresh(). Uses a 100ms minimum delay to prevent flash of loading content and ensure the indicator is observable. Initial user state starts as null; authRefresh validates the stored token server-side. Learned: React 18 batches effect state updates so fast they can be invisible to Playwright — a minimum delay ensures the loading state is paintable.
[2026-02-15] REQ-AUTH-006 verified. Auth persistence already works — PocketBase stores auth in localStorage (pocketbase_auth key), which survives page refreshes. authRefresh in AuthProvider re-validates the token on reload. No new code needed.
[2026-02-15] REQ-AUTH-007 implemented. LoginPage now has sign-in/sign-up mode toggle. Uses anchor links (<a href="#">) for switching modes. Sign-up form adds Confirm Password field. Learned: Vite HMR in Docker sometimes doesn't pick up large file changes — restart the client container if the page serves stale code.
[2026-02-15] Fixed bug in LoginPage.tsx: sign-up form referenced undefined `handleSubmit` instead of `handleSignUp`, causing a ReferenceError that crashed the component when switching to sign-up mode. All 10 e2e tests now pass. Marked REQ-AUTH-008 as passing.

